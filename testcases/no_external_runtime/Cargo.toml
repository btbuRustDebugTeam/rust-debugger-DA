[package]
name = "no_runtime_futures"
version = "0.1.0"
edition = "2021"

[dependencies]
# No external async runtime - we build everything from scratch

[profile.dev]
opt-level = 0
debug = 2  # Full debug info for DWARF analysis
