# 进度同步

## 每周同步

每周同步采用 `本周完成的工作` — `遇到的困难` — `下一步计划` 的格式进行记录

### Start ~ 09.07

| 本周完成的工作                                               | 遇到的困难                                                   | 下一步计划                                       |
| ------------------------------------------------------------ | ------------------------------------------------------------ | ------------------------------------------------ |
| ① 找 xy 老师询问适合的内核测试样例，尝试 embassy，但因 qemu 模拟不佳未成功；改用 lilos 进行测试；<br />② 完成多运行时测试用例 | ① embassy 需要的板子 qemu 模拟不好；<br />② 多运行时测试用例选择不好，很多函数是调试器边界情况，需要重写 | ① 继续推进 lilos 测试；<br />② 复现 radare2 界面 |

### 09.08 ~ 09.14

| 本周完成的工作                                               | 遇到的困难                                                   | 下一步计划                                                   |
| ------------------------------------------------------------ | ------------------------------------------------------------ | ------------------------------------------------------------ |
| ① 成功运行 lilos 的 testsuite/lm3s6965 测试用例；<br />② 在调试器 Makefile 中添加 lilos 调试命令；<br />③ 完成多运行时测试；<br />④ 调研 Tock OS 是否涉及跨硬件特权级调试 | ① 当前只能证明调试器能调试内核态程序，还无法证明支持特权级切换的程序调试 | ① 复现 radare2 界面研究，应用到工具中；<br />② 复现 embassy 调试器 |



### 09.22 ~ 10.12

| 本周完成的工作                                               | 遇到的困难                                            | 下一步计划                                                   |
| ------------------------------------------------------------ | ----------------------------------------------------- | ------------------------------------------------------------ |
| ① 学习 radare2 使用和 UI 设计；<br />② 为 code-debug 编写 uCore 配置文件，支持 lab1-lab8 调试，添加 lab 选择下拉菜单 | ① uCore 的编译启动单位是 lab，需要为每个 lab 单独配置 | ① 弄清楚 VS Code 和 GDB 之间的信息传输，将工具集成到 VS Code 中；<br />② 后续再优化界面 |

### 10.13 ~ 10.19

| 本周完成的工作                                               | 遇到的困难                                               | 下一步计划                             |
| ------------------------------------------------------------ | -------------------------------------------------------- | -------------------------------------- |
| ① 学习 czy 学长毕设中的集成思路，明确通过实现 Debug Adapter（DA）将调试器集成到 VS Code；<br />② 了解 VS Code 扩展开发框架（yeoman、generator-code）和 DAP 协议 | ① 对 VS Code 扩展开发和 DAP 协议刚开始接触，需要学习成本 | ① 创建扩展框架；<br />② 实现调试适配器 |

### 10.20 ~ 10.26

| 本周完成的工作                                               | 遇到的困难                                                   | 下一步计划                                                   |
| ------------------------------------------------------------ | ------------------------------------------------------------ | ------------------------------------------------------------ |
| ① 复现 rCore 调试工作，解决 release 模式编译优化导致断点偏移问题，改用 debug 编译参数；<br />② 排查 rCore 用户态断点无法命中的问题，发现 getStringVariable 函数 bug 并修复；<br />③ 解决 initproc 断点被 user_shell 覆盖的问题，通过取消 process.rs:49 钩子解决问题 | ① 学长补丁和新版 rCore 仓库均不可用；<br />② 调试器来不及切换断点组，initproc.rs 程序已运行完；<br />③ uCore 每个 lab 边界断点和钩子行号不同，需为每个 lab 编写配置，但对其不熟悉 | ① 解决零号进程无法打断点的问题；<br />② 解决补丁问题；<br />③ 写自动关闭 VS Code 终端的功能 |

### 10.27 ~ 11.02

| 本周完成的工作                                       | 遇到的困难 | 下一步计划 |
| ---------------------------------------------------- | ---------- | ---------- |
| 本周因准备发展对象材料和两门课结课考试，暂停任务进度 | —          | —          |

### 11.03 ~ 11.09

| 本周完成的工作                                               | 遇到的困难                           | 下一步计划                                   |
| ------------------------------------------------------------ | ------------------------------------ | -------------------------------------------- |
| ① 解决 QEMU 端口冲突导致重启调试失败的问题，在 gdb.ts 中添加 cleanupQemuProcess 方法，调试结束时强制关闭 QEMU 进程；<br />② 排查调试会话提前结束的问题（调试用户态程序 yield.rs 时断点切换流程异常） | ① 调试会话提前结束的具体原因尚不明确 | ① 调试调试器本身；<br />② 添加多进程调试支持 |

### 11.10 ~ 11.16

| 本周完成的工作                                               | 遇到的困难                                        | 下一步计划                   |
| ------------------------------------------------------------ | ------------------------------------------------- | ---------------------------- |
| ① 学习调试器本身的调试方法：code-debug 分为 extension 和 DA 两个进程，需分别启动调试配置；<br />② 开始调试断点无法命中的问题，发现断点绑定路径有问题（gdb.js.map 中 sources 路径多了 ../），修改 tsconfig.json 的 rootDir | ① 修改 rootDir 后编译结果正确但仍提示无法绑定断点 | ① 继续排查断点无法绑定的根因 |

### 11.17 ~ 11.23

| 本周完成的工作                                               | 遇到的困难                                                   | 下一步计划                                                   |
| ------------------------------------------------------------ | ------------------------------------------------------------ | ------------------------------------------------------------ |
| ① 解决一号进程无法调试的问题（代码中函数使用 bug）；<br />② 重新 clone 调试器代码成功调试，发现之前添加的自动结束 QEMU 进程功能导致调试失败，暂时放弃该方案；<br />③ 处理「Selected thread is running」被误判为 error 导致崩溃的问题，在 onOutput 中拦截并改为 done；<br />④ 处理 GDB 多种线程运行提示，改为宽泛匹配「thread is running」并转小写；<br />⑤ 处理返回空数组导致上层函数崩溃的问题，在 doAction 所有调用处添加空值检查 | ① 新增的自动结束 QEMU 进程功能导致调试器无法调试，具体原因不明 | ① 编译和发布调试器；<br />② 上传 rCore 调试补丁；<br />③ 修改调试方法文档，录制调试视频教程；<br />④ 有时间适配 uCore 调试 |

### 11.24 ~ 12.07

| 本周完成的工作                                               | 遇到的困难                                               | 下一步计划                                                   |
| ------------------------------------------------------------ | -------------------------------------------------------- | ------------------------------------------------------------ |
| ① 分解调试器 VS Code 集成任务：自动化 JSON 配置、创建插件框架、编写 DA 核心、集成 LLVM opt 分析（使用 preLaunchTask）；<br />② 实现 GDB 控制器，支持 MI 协议启动和命令交互；<br />③ 解决 Python 依赖问题（内置 pyelftools 和 networkx，自动注入 sys.path）；<br />④ 修复路径相关 bug（命令引号转义、自动创建 async_trace_results 文件夹）；<br />⑤ 实现完整启动流程：自动加载符号、查找 poll 函数、设置白名单、启动异步分析 | ① LLVM opt 分析耗时长，需通过 preLaunchTask 避免界面卡死 | ① 让插件支持 VS Code 打断点；<br />② 获取堆栈信息并显示在调用堆栈中 |

### 12.08 ~ 12.14

| 本周完成的工作                                               | 遇到的困难                                   | 下一步计划                                                   |
| ------------------------------------------------------------ | -------------------------------------------- | ------------------------------------------------------------ |
| ① 重构 Makefile 逻辑，划分 deps、gdb、run 等功能模块；<br />② 修改 config.py，统一动态拼接 cwd 路径，确保调试产物输出到 async_trace_results/；<br />③ 修复 load_async_dependencies() 文件名错误（async_dependencies.json → async_deps.json）；<br />④ 删除白名单逻辑，准备重构插件逻辑为通过 VS Code 断点控制调试流程 | ① 白名单配置方式需要改为更直观的断点控制方式 | ① 协程调用栈可展开显示；<br />② 实现内核态和用户态断点；<br />③ 异步/同步函数断点命中后在调用栈显示；<br />④ 火焰图功能 |

### 12.15 ~ 12.21

| 本周完成的工作                                               | 遇到的困难               | 下一步计划                                  |
| ------------------------------------------------------------ | ------------------------ | ------------------------------------------- |
| ① 编写扩展设计文档；<br />② 规划四个开发阶段：基础 Webview 面板 → 动态渲染 → inspect-async 调用栈 → 火焰图等余下功能 | ① 需要逐步推进四阶段开发 | ① 开始阶段一：创建独立 VS Code Webview 面板 |

### 12.22 ~ 12.28

| 本周完成的工作                                               | 遇到的困难                                       | 下一步计划                  |
| ------------------------------------------------------------ | ------------------------------------------------ | --------------------------- |
| ① 完成自动化调试前初始化工作，打通 GDB 启动、Python 脚本加载、DWARF 分析、poll 函数查找、白名单生成、异步追踪启动的完整链路；<br />② 实现了 poll_map.json 自动生成和异步依赖展开 | ① 部分同步函数无法通过调用图正确映射到 poll 函数 | ① 继续推进 Webview 面板开发 |

### 01.12 ~ 01.18

| 本周完成的工作                                               | 遇到的困难                 | 下一步计划                                           |
| ------------------------------------------------------------ | -------------------------- | ---------------------------------------------------- |
| ① 尝试使用最新版调试器，理解白名单过滤 + ardb-trace 设置跟踪起点的调试逻辑；<br />② 设计目标 UI 界面；<br />③ 修改 ardb-gen-whitelist 运行逻辑，将白名单文件从调试器 temp 目录改为被调试程序工作目录 | ① 白名单生成位置不便于封装 | ① 修改文档逻辑；<br />② 整合异步调试和特权级切换功能 |

### 01.19 ~ 01.25

| 本周完成的工作                                               | 遇到的困难                               | 下一步计划                                                   |
| ------------------------------------------------------------ | ---------------------------------------- | ------------------------------------------------------------ |
| ① 关闭终端实时调用栈输出，只输出程序结果，调用栈写入 ardb.log 文件；<br />② 实现 ardb-get-snapshot 命令，获取当前断点处的异步调用栈快照（JSON 格式，包含协程 ID、poll 次数、状态等）；<br />③ 编写 DA 设计文档 | ① 需要区分同步和异步调用栈的快照展示格式 | ① 撰写 Overleaf 文档；<br />② 实现火焰图功能；<br />③ 跨特权级功能设计 |

### 01.26 ~ 02.01

| 本周完成的工作                                               | 遇到的困难                                                   | 下一步计划                                                   |
| ------------------------------------------------------------ | ------------------------------------------------------------ | ------------------------------------------------------------ |
| ① 解决调试器启动时卡死在加载界面的问题，打通插件与 VS Code 通信；<br />② 修复 UI 界面脚本崩溃问题，Webview 面板按钮可正常响应；<br />③ 实现从界面触发 GDB 扫描代码符号并生成 poll_functions.txt 白名单；<br />④ 打通「Webview → 插件 → DA → GDB Python 脚本」后端链路；<br />⑤ 实现 F5 启动后调试器自动进入暂停状态，在程序运行前完成 Reset 和 Trace 配置 | ① 白名单生成后，Trace 入口设置后是否能准确捕获异步 Poll 事件并生成执行树还需测试；<br />② UI 树图渲染待测试 | ① 优化快照用词；<br />② 设计调用栈展示逻辑（用户设置入口、展示深度）；<br />③ 让其他同学复现火焰图 |

### 03.01 ~ 03.08

| 本周完成的工作                                               | 遇到的困难                       | 下一步计划                                                   |
| ------------------------------------------------------------ | -------------------------------- | ------------------------------------------------------------ |
| ① 完成 GDB MI2 输出解析器（识别 stopped、running、done/error、console stream、通知等消息类型）；<br />② 完成 DAP configurationDone + 自动 run（F5 启动后程序自动运行，命中入口断点 VS Code 显示暂停状态）；<br />③ 完成 threads + stackTrace 请求（暂停时 VS Code 左侧显示线程列表和调用栈，点击帧可跳转源码）；<br />④ 完成 variables 请求（变量面板正确显示局部变量和参数）；<br />⑤ 完成执行控制（continue/next/stepIn/stepOut/pause 全部可用）；<br />⑥ 完成 setBreakpoints 请求（VS Code 编辑器行号区点击设置断点并正确命中）；<br />⑦ 完成 evaluate 请求完善（Debug Console 可输入 GDB 命令并看到真实输出）；<br />⑧ 完成 stopped 事件驱动自动快照 + Webview 联动（移除 500ms 轮询，事件驱动刷新异步执行树） | ① 八个任务按依赖关系需串并行推进 | ① 左下角堆栈改为我们的逻辑调用栈数据；<br />② 树上函数点击能到对应程序行数；<br />③ 优化白名单业务；<br />④ 异步 Rust 操作系统调试；<br />⑤ 制作使用文档网页 |

### 03.09 ~ 03.15

| 本周完成的工作                                               | 遇到的困难                                                   | 下一步计划                                                   |
| ------------------------------------------------------------ | ------------------------------------------------------------ | ------------------------------------------------------------ |
| ① 异步函数可点击跳转到源码；停在断点处树自动更新；VS Code 原生 Call Stack 面板内容改为逻辑调用栈；变量面板功能实现；<br />② 搭建推广网站和内容；<br />⑥ 完成白名单优化设计：提出三层设计（Trace 根、白名单按 crate 分组默认跟踪用户代码、底层 poll 函数自动处理），将白名单定位从「用户手动选择」改为「过滤噪音」 | ① 同步函数入口作为 Trace 根时 CID、poll 次数、addr 内容有问题 | ① 跨特权级跟踪功能；<br />② 将 VS Code 原生调用堆栈加入插件；<br />③ eBPF 动态跟踪调研；<br />④ 编写调试器使用文档 |

### 03.16 ~ 03.22

| 遇到的困难                                                   | 本周完成的工作                                               | 下一步计划                                                   |
| ------------------------------------------------------------ | ------------------------------------------------------------ | ------------------------------------------------------------ |
| ① Trace 入口设为同步函数时 CID、poll 次数、addr 有问题（同步函数无 poll 逻辑无法初始化影子栈；指针解引用逻辑无法处理 Pin 等 Rust 包装类型）；<br />② Trace 按钮功能不够直观 | ① 异步调试DA 细节优化：把白名单功能合并到 Webview 中，按 crate 分类展示，支持 Apply Whitelist 和 Trace 按钮；将「Async Tree」面板改名为「Logical Call Tree」以包含同步函数；异步函数多次 poll 时不产生新节点而是增加 poll 计数值；<br />② 对物理调用栈和逻辑调用栈拆分展示进行设计分析； | ① 先把 code-debug 功能全部加进来，再抠细节；<br />② 实验验证物理调用栈和逻辑调用栈是否一致；<br />③ 优化 Trace 功能交互 |

### 03.23 ~ 03.29（第二阶段—OS功能移植至 async-debug）

| 本周完成的工作                                               | 遇到的困难 | 下一步计划                                                   |
| ------------------------------------------------------------ | ---------- | ------------------------------------------------------------ |
| ① 完成 code-debug 功能拆解；<br />② 写出功能移植清单和方案：升级 DAP（标准 DAP 替代手写 DAP）、移植 OS 状态机、实现 attach 模式和 QEMU 支持、OS + 异步联合调试；<br />③ 进行初步 DAP 升级 | 无         | ① async-debug 适配 embassy（RISC-V）；<br />② 修复白名单加载路径问题；<br />③ 修复调试 embassy 项目路径问题 |

### 03.30 ~ 04.05

| 本周完成的工作                                               | 遇到的困难                                                   | 下一步计划                                       |
| ------------------------------------------------------------ | ------------------------------------------------------------ | ------------------------------------------------ |
| ① 修复两个 bug：Reset 按钮未清除 Whitelist 面板、调用树在 trace root 未触发前就生成；<br />② 完成 MI 协议层移植，替换之前的手写 MI 解析器；<br />③ 完成 OS 状态机移植 及 BreakpointGroups 提取；<br />④ 编写 37 个 OSStateMachine 单元测试 | ① 断点均处于未验证，F5 未在断点处停下（断点设置时机问题：GDB 还未加载对应符号文件） | ① 解决断点验证问题；<br />② 尝试调试 OS 调试流程 |

### 04.06 ~ 04.12

| 本周完成的工作                                               | 遇到的困难                                                   | 下一步计划                     |
| ------------------------------------------------------------ | ------------------------------------------------------------ | ------------------------------ |
| ① 处理未验证断点问题，成功实现断点组状态切换（内核态断点验证变红，进入用户态后对应用户程序断点组变红）；<br />② 排查调试 rCore 时卡在 initproc 程序、无法到达 user_shell.rs 的问题 | ① 调试 rCore 时卡在 initproc 里，到不了 user_shell，原因正在排查 | ① 继续排查 initproc 卡住的问题 |

### 04.13 ~ 04.19

| 本周完成的工作                                               | 遇到的困难                                                  | 下一步计划                                               |
| ------------------------------------------------------------ | ----------------------------------------------------------- | -------------------------------------------------------- |
| ① 按老师要求转移仓库：code-debug → osgdb（跨特权级）、原DA仓库改名为 async-debug、移植 文档仓库；<br />② 整体架构梳理文档：MI2、OSStateMachine、BreakpointGroups、GDBDebugSession 各模块职责和完整数据流 | ① Hook 断点加载逻辑有问题，但具体位置找了很久未定位；<br /> | ① 继续研究 code-debug 中的逻辑，解决多进程 trap 区分问题 |

### 04.20 ~ 04.26

| 本周完成的工作                                               | 遇到的困难             | 下一步计划                                 |
| ------------------------------------------------------------ | ---------------------- | ------------------------------------------ |
| ① 完成仓库迁移（code-debug → osgdb）；<br />② 继续排查 trap_return 卡住的问题，注入 GDB 收发日志定位；<br />③ 猜测根因：rCore 多进程系统中 trap 无法区分进程，导致符号文件切换混乱和无限循环（后续验证猜想错误） | ① 尝试多种解决办法未果 | ① 重新深入研究 code-debug 中的原始实现逻辑 |

### 04.27 ~ 05.02

| 本周完成的工作                                               | 遇到的困难                                                   | 下一步计划                            |
| ------------------------------------------------------------ | ------------------------------------------------------------ | ------------------------------------- |
| ① 修复 code-debug 的两个 bug：边界断点异常停下、断点切换失败；<br />② 听 2026 开源操作系统训练营实习总结报告，终点了解了：AXCI 自动化测试系统、AxVisor eBPF Tracing 技术 | ① 原有 code-debug 的 bug导致async-debug 整合 OS 调试功能时遇到阻碍 | ① 继续在async-debug中整合 OS 调试功能 |

### 05.03 ~ 05.09（第三阶段—osgdb 适配 Starry OS）

| 本周完成的工作                                               | 遇到的困难                                                   | 下一步计划                                   |
| ------------------------------------------------------------ | ------------------------------------------------------------ | -------------------------------------------- |
| ① 重新梳理 OS 调试逻辑链梳理代码，帮助移植调试功能<br />② 发现之前设置 border/hook 断点用的是旧 code-debug 插件面板按钮，而非当前 async-debug 的命令面板，纠正后边界断点和 hook 成功工作；<br />③ 解决运行到用户态时调试器卡在单步判断的问题（launch.json 中 border 配置有误）；<br />④ 解决卡在 initproc 无法到 user_shell 的问题，修复了多个 hook 相关 bug：hook 报错被 try/catch 静默吞掉；toFunctionString 忽略 isAsync 标志导致 await 语法错误；getStringVariable API 移植遗漏；hook 断点位置 path 已不是裸指针导致 GDB x/s 失败；实现新的 getStringVariable（通过 GDB 读 String 内部布局获取字符串内容） | ① hook 行为函数执行失败被静默捕获，难以排查；<br />② Rust String 内部布局依赖（.vec.buf.ptr.pointer.pointer+.vec.len），rustc 改字段路径会失效 | ① osgdb适配Starry OS；<br />② async OS的适配 |

### 05.10 ~ 05.16

| 本周完成的工作                                               | 遇到的困难                                                   | 下一步计划                                                   |
| :----------------------------------------------------------- | ------------------------------------------------------------ | ------------------------------------------------------------ |
| ① osgdb 功能修复：将 code-debug fork到新的开发仓库 osgdb 中，并修复一些小的问题，成功调试 rCore；<br />② 尝试 uCore 适配：尝试编写 launch.json 配置；<br />③ 完成 StarryOS 初步配置；<br />④ 设计并实现注释标记断点新功能，支持在源码中通过 @osgdb:border / @osgdb:hook 注释标记断点位置，自动扫描替代手写 filepath+line | ① uCore 最新代码在 LearningOS/uCore-Tutorial-Code，旧版无法启动，uCore 是按章节设计的，并且user 代码在另外的仓库，这都增大了设计配置文件的难度；<br />② StarryOS border 断点位于外部 crate，无本地源码路径；<br />③ trap_vectorbase 作为 border 会被内核启动期间的频繁 trap 触发，导致启动极慢；<br />④ StarryOS 与 rCore 的本质不同，导致边界断点和hook断点都需要进行相应的适配。 | ① 继续 StarryOS border 断点问题排查（函数名 border 命中后直接暂停而未进入单步模式穿越地址边界）；<br />② 写配置文件编写方法文档，帮助后续的开发者快速理解和实现； |

### 05.17 ~ 05.23

| 本周完成的工作                                               | 遇到的困难                                                   | 下一步计划                                                   |
| ------------------------------------------------------------ | ------------------------------------------------------------ | ------------------------------------------------------------ |
| ① 修复 osgdb 边界断点 bug：<br />函数名 border 断点生命周期管理遗漏（状态切换时未被 BreakpointGroups 清理，导致残留断点被误判为普通断点）；<br />引入边界断点方向字段（Direction: UserToKernel / KernelToUser）解决状态机单向停滞问题；<br />② 详细分析 rCore vs StarryOS 在边界断点设计上的差异；<br />③ 排查 hook 断点无法获取文件名的问题：path 变量被编译器优化（debug 版启动不了，release 版变量被优化） | ① StarryOS debug 版编译产物太大加载极慢或卡住；<br />② Cargo.toml 修改编译优化级别仍无法保留 path 变量（不太确定） | ① 咨询 StarryOS debug 版启动问题；<br />② 上传开发日志；<br />③ 调试打通后实现调试器自动识别边界断点和 hook 断点（无人介入） |

### 05.24 ~ 05.30

| 本周完成的工作                                               | 遇到的困难                                                   | 下一步计划                                                   |
| ------------------------------------------------------------ | ------------------------------------------------------------ | ------------------------------------------------------------ |
| ① 修复边界断点两个 bug：user 态缺少 AT_KERNEL 处理器导致 user→kernel 状态转换失败；function-based border 断点只注册到 kernel 组，user 态无边界断点导致 user→kernel 无法触发；<br />② 排查 hook 断点 getStringVariable('path') 失败问题（Rust String 内部布局通过 GDB 读取 .vec.buf.ptr.pointer.pointer + .vec.len 获取字符串内容）；<br />③ 排查 launch.json 断点组名称不匹配问题：filePathToBreakpointGroupNames 归类为 user 组，但 execve hook 返回 ./test_user，导致切换到空组（N=0） | ① StarryOS debug 版编译产物太大加载极慢或卡住；<br />② 用户断点组名称一致性需要仔细对齐各配置函数 | ① 继续 StarryOS 适配；<br />② 整理工作内容，规划如何放入初赛 |

### 06.01 ~ 06.07

| 本周完成的工作                                               | 遇到的困难                                                   | 下一步计划             |
| ------------------------------------------------------------ | ------------------------------------------------------------ | ---------------------- |
| ① osgdb 适配 StarryOS 成功，用户程序断点正常命中；<br />②整理开发工作文档 | ① 用户程序编译时未加调试参数，且需在ubuntu中手动编译用户程序并挂载到StarryOS磁盘镜像，流程较繁琐 | ① 撰写参赛文档；<br /> |

### 06.08 ~ 06.14

| 本周完成的工作                                               | 遇到的困难                                                   | 下一步计划             |
| ------------------------------------------------------------ | ------------------------------------------------------------ | ---------------------- |
| ① 整理初赛创新、完成情况等内容；<br />②整理参赛仓库并提交； | 无 | ① 继续撰写参赛文档；<br /> |

### 06.15 ~ 06.21

| 本周完成的工作                                               | 遇到的困难                                                   | 下一步计划             |
| ------------------------------------------------------------ | ------------------------------------------------------------ | ---------------------- |
| ① 提炼出了整体创新作为文档依据；<br />②初赛文档初步撰写完成； | 无 | ① 继续完善参赛文档；<br /> ②录制演示视频；|

